# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/
OPENROUTER_API_KEY= 

# Google OAuth Configuration
# Get these from: https://console.developers.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Authentication Configuration
# Generate a secure random string for session encryption
BETTER_AUTH_SECRET=
# The base URL where your app is deployed (used for authentication callbacks)
BETTER_AUTH_URL=

# Database Configuration
# PostgreSQL connection string
DATABASE_URL=

# Public Configuration
# The public URL of your application (used for API references and CORS)
NEXT_PUBLIC_BASE_URL=

# Redirect URL after authentication
REDIRECT_URL=

# Development Notes:
# When running a development build (e.g. `npm run dev`):
# - Public pages are automatically redirected to /dashboard
# - Use http://localhost:3000 for local development URLs
